packages:
  - apps/*
  - packages/*

# Allow these dependencies to run install/postinstall scripts (native binaries + Prisma engines).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true

minimumReleaseAgeExclude:
  - '@anthropic-ai/sdk@0.109.0'
