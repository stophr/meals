# PWA icons

`vite.config.ts`'s PWA manifest references `/icon-192.png` and `/icon-512.png`.
Drop real PNGs of those sizes here before shipping — until then the app still builds
and runs, but the installed-app icon falls back to the browser default.
