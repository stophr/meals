# Pantrezy (meals)

Recipe-first household grocery planning monorepo. pnpm workspace:
apps/api (Fastify+Prisma), apps/web (Vite/React), packages/{core,db,ingestion,shared}, services/paddleocr.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`stophr/meals`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
